# Core dependencies
biopython>=1.79
pandas>=1.3.0
scipy>=1.7.0
numpy>=1.21.0
matplotlib>=3.4.0
seaborn>=0.11.0
jinja2>=3.0.0
edlib>=1.3.9

# Geographic analysis (optional but recommended for marine organisms)
# Uncomment the lines below if you need geographic analysis:
# geopandas>=0.10.0
# cartopy>=0.20.0
# shapely>=1.8.0
# pyproj>=3.0.0

# Note: External tools (MAFFT, trimAl, FastTree) must be installed separately
# Install via conda: conda install -c bioconda mafft trimal fasttree
# Or via system package manager
